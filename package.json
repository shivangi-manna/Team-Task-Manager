{
  "name": "team-task-manager",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "cd backend && npm install --omit=dev && cd ../frontend && npm install && npm run build",
    "start": "cd backend && node server.js"
  }
}
