{
  "name": "team-task-manager",
  "version": "1.0.0",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "postinstall": "cd backend && npm install --omit=dev && cd ../frontend && npm install",
    "build": "cd frontend && npm run build",
    "start": "cd backend && node server.js"
  }
}
